'use strict';

// TODO: create the following routes

/*
/crawls - GET - retrieves all saved crawls for all users
/crawls/:user/:id - DELETE - deletes a single crawl
/crawls - POST - creates a new crawl
/crawls/:user/:id - GET - retrieves single crawl on user's profile
/crawls/:user - GET - retrieves all crawls for single user
/crawls/:id - PUT - updates a single crawl (adding votes)
/crawls/:id/votes - GET - retrieves total number of votes on crawl

 */